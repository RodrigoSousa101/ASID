apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-db
  template:
    metadata:
      labels:
        app: cart-db
    spec:
      containers:
        - name: cart-db
          image: mariadb:11
          ports:
            - containerPort: 3306
          env:
            - name: MARIADB_ROOT_PASSWORD
              value: "1234"
            - name: MARIADB_DATABASE
              value: "cartdb"
---
apiVersion: v1
kind: Service
metadata:
  name: cart-db
spec:
  selector:
    app: cart-db
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  type: ClusterIP
